APP_ENV=dev
WG_ADDR=127.0.0.1

# encode backend: stub (fake vectors, no model needed) | onnx (models in ./models)
BACKEND=stub
# dev: let empty accounts run inference
BILLING_ENFORCE=false

# postgres
POSTGRES_USER=aime
POSTGRES_PASSWORD=changeme
POSTGRES_DB=aime
POSTGRES_PORT=5432

# grafana admin login
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme
