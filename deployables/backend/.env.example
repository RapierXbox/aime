HTTP_ADDR=:8080
APP_ENV=dev

# stub = deterministic fake vectors, no model files needed. onnx needs MODEL_DIR/{embed,rerank}/{model.onnx,tokenizer.json}
BACKEND=stub
MODEL_DIR=./models
# EMBED_POOLING=cls        # cls for bge, mean for e5/minilm
# ONNXRUNTIME_LIB=         # path to libonnxruntime.so / onnxruntime.dll, empty = default lookup
# INFER_CONCURRENCY=2
# INFER_TIMEOUT=30s

# dev: let empty accounts run inference (usage is still recorded)
BILLING_ENFORCE=false
# only true behind caddy, it makes the rate limiter read X-Forwarded-For
TRUST_PROXY=false

# backups: per file cap, dir, versions per account, time allowed for one transfer
MAX_BACKUP_MB=5000
BACKUP_DIR=./backups
# BACKUP_MAX_VERSIONS=10
# BACKUP_TIMEOUT=30m
# BACKUP_CONCURRENCY=4      # uploads in flight server wide
# BACKUP_TOTAL_CAP_GB=0     # all accounts together, 0 = unlimited
# AUTH_CONCURRENCY=4        # argon2 hashes at once, 64MiB each

# Matches the defaults in root .env.example; requires `task db:up` first
DATABASE_URL=postgres://aime:changeme@localhost:5432/aime?sslmode=disable
