name: "aime"

services:
  db:
    image: "postgres:18" # major pin: a bump refuses the existing data dir
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-aime}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-aime}
      POSTGRES_DB: ${POSTGRES_DB:-aime}
    volumes:
      - db-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-aime} -d ${POSTGRES_DB:-aime}"]
      interval: 5s
      timeout: 5s
      retries: 10
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432" # local debugging only
    
  backend:
    build:
      context: ../../deployables/backend
    restart: unless-stopped
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    depends_on:
      db:
        condition: service_healthy
    environment:
      HTTP_ADDR: ":8080"
      APP_ENV: ${APP_ENV:-dev}
      BACKEND: ${BACKEND:-onnx}
      MODEL_DIR: /models
      EMBED_POOLING: ${EMBED_POOLING:-cls}
      BILLING_ENFORCE: ${BILLING_ENFORCE:-true}
      TRUST_PROXY: "true" # only reachable through caddy
      BACKUP_DIR: /backups
      MAX_BACKUP_MB: ${MAX_BACKUP_MB:-5000}
      BACKUP_TOTAL_CAP_GB: ${BACKUP_TOTAL_CAP_GB:-0}
      DATABASE_URL: postgres://${POSTGRES_USER:-aime}:${POSTGRES_PASSWORD:-aime}@db:5432/${POSTGRES_DB:-aime}?sslmode=disable
    volumes:
      - ${MODELS_DIR:-../../models}:/models:ro # unused with BACKEND=stub
      - backup-data:/backups
    expose:
      - "8080"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/readyz || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s
  caddy:
    build:
      context: ../../deployables/frontend # caddy image with the built spa in /srv
    profiles: ["edge"] # not needed for local dev for now
    restart: unless-stopped
    depends_on: 
      backend:
        condition: service_healthy
    ports:
      - "127.0.0.1:80:80"
    volumes:
      - ../caddy/Caddyfile:/etc/caddy/Caddyfile:ro

  prometheus:
    image: prom/prometheus:latest
    profiles: ["monitoring"]
    restart: unless-stopped
    volumes:
      - ../prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    expose:
      - "9090" # internal only
  
  grafana:
    image: grafana/grafana:latest
    profiles: ["monitoring"]
    restart: unless-stopped
    depends_on: [prometheus]
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
    volumes:
      - ../grafana/provisioning:/etc/grafana/provisioning:ro
      - grafana-data:/var/lib/grafana
    ports:
      - "127.0.0.1:3000:3000" # localhost only in base
    
volumes:
  db-data:
  backup-data:
  prometheus-data:
  grafana-data:
