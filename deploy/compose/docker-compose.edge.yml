# server edge overlay... makes services reachable over wg
services:
  caddy:
    ports:
      - "${WG_ADDR:?WG_ADDR must be the wireguard ip, an empty value would bind every interface}:80:80"
  grafana:
    ports:
      - "${WG_ADDR:?WG_ADDR must be the wireguard ip}:3000:3000"