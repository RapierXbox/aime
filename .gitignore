# env
.env
deployables/backend/.env
deployables/frontend/.env

# models are downloaded, never committed
models/
# local backup blobs
backups/

# for now :)
docs/

# go
deployables/backend/bin/
deployables/backend/server

# ansible rendered + secrets
deploy/caddy/Caddyfile
ansible/inventory/group_vars/all/vault.yml
*.retry
CLAUDE.md
