# src-tauri dev .env — copy to .env (gitignored): cp .env.example .env

# MACOS ONLY:
# Your macOS login password. Used by dev-runner-macos.sh to re-authorize each
# rebuild against the "aime" keychain item so the Keychain stops re-prompting.
# Only your local machine ever reads this; .env is gitignored.
KEYCHAIN_PW=

# Rust backtraces in development
RUST_BACKTRACE=1
